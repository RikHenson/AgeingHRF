onsets.tar.gz is a gzipped tarball containing one *.csv file per participant,
with each row in that file representing one trial and the columns representing
onset and duration (in seconds from start of run) and trial-type (BIDS).