For the matlab scripts to work, you need to extract files from
"onsets.tar.gz", which should produce one *.csv file per participant,
with each row in that file representing one trial and the columns 
representing onset and duration (in seconds from start of run) and 
trial-type (BIDS).